# Server Configuration
NODE_ENV=development
PORT=5000

# MongoDB Connection
MONGO_URI=mongodb://localhost:27017/loan-manager

# JWT Authentication (if implementing auth in the future)
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRE=30d

# Client URL
CLIENT_URL=http://localhost:3000
